### Класс с `__del__`
Создай класс `Logger`, который при создании объекта выводит `Создан логгер`, а при удалении — `Удалён логгер`.

Пример:
```python
l = Logger()
del l
# Вывод:
# Создан логгер
# Удалён логгер
```